# Preparation candidate scan validation

Candidate changes only private history search in DocumentProfile, based on pinned778997e. Local CPU probe already showed unchanged preparation uses~3.27M/6.52M reductions at64/128 synthetic rows and slows under a throttled local quota. Hosted preparation had no completion mark4299ms after entry; this candidate is not yet tested or deployed, and CPU sensitivity does not establish the hosted cause.

Exact retry must remain byte equality. The candidate decodes all existing rows as before, indexes rows once, and skips positions whose first entry is not v2 with the requested operation ID ONLY when a successful largest-positional-start build certifies all earlier deterministic candidate validations. All parsed predecessor IDs must be valid lowercase ASCII UUIDs; generated IDs are fixed36 bytes; sequence1 nil is shorter; for positive safe integer sequences the decimal width at each batch index cannot exceed its largest-start width. Bodies/log/writer/time/operation are fixed. Under pinned Entry/Jcs, this bounds canonical size and unchanged field validity. This is a source-specific proof, not a generic cache assumption. Never use history's supplied writer_seq instead of original positional coordinates.

Any failed/uncertain certificate, including returned error or native error/throw/exit, falls back to the original unfiltered scan. No speculative failure is returned. This matters at size boundaries: an earlier exact replay can fit while a later candidate exceeds1MiB. The original scan retains first-error/first-match behavior; no catch wraps its native exceptions. Whole-history decode remains. Successful certification does not promise identical resource-exhaustion or external operational-fault timing after skipped work. Existing public API/options/handle/prepared bytes/lease/commit path remain unchanged.

The frozen original module fixture is byte-equal Git778997e; tests rename only its outer module for differential execution. Ten new tests cover batch>1 and9/10/99/100 widths; mixedv1/v2/reusedoperation/body/time/partialbatch; earlier seq1 andseq9 max-size replay despite certificate failure; first candidate too-large error; malformed JSON/missing/invalid predecessor; native encoding exception fallback; empty/short history matrix; and valid equal results with fewer than half the baseline reductions at64/128 rows. Reduction threshold tests demonstrated eliminated work, not timing/cure. One tagged baseline performance test must fail on unchanged source before the full candidate run. No synthetic throw/exit fault hook is added to the pure builder; the certificate catch's full kinds are source-reviewed, real native encoding error is runtime-tested.

Candidate full suite:21 existing actualSQLite profile cases +4 existing sidecar protocol cases using in-memory loopback transport +10 new differential cases =35. Library source is compiled from this checkout before startup, with cached dependency beams bound in INPUTS. Only commonplace_log starts; realm HTTP environment is absent, no hosted credentials/app/browser/Docker/cloud. The existing SQLite tests create and remove their own temporary data directories; originals from other runs remain untouched.

Boss alone ONE command after Plan source no-blocker and fresh gates, from /home/jes/commonplace-log-prepare-scan:

```sh
/usr/bin/env -i PATH=/usr/bin:/bin LANG=C.UTF-8 PYTHONDONTWRITEBYTECODE=1 /usr/bin/timeout --signal=TERM --kill-after=5s 180s /usr/bin/python3.12 -B commonplace_log/docs/measurements/prepare-scan-1/run.py /home/jes/commonplace-log-prepare-scan/tmp/validation-1
```

Detached owner190s with original SHA/HEAD/argv/streams/rc/UTC retained in a separate fresh private audit. Runner creates naturally absent tmp/validation-1, no precreate/move/reuse. Same reviewed owned child process-group cleanup; two75s children/internal175s/outer180+5. Every launch requires child+cleanup reserve. PRE manifest verification gates, equal valid POST retained, original child codes and known group absence required. Physical interruption can prevent completion; incomplete is not accepted. Post-read stdout/stderr1MiB caps are not hard allocation limits.

Acceptance: baseline native1/exact10total1failure9excluded0skipped and named performance case; two retained metrics proving valid equal prepared results but failing the reduction threshold. Candidate native0/35total0failure0excluded0skipped, two valid equal prepared outputs and reduction improvement; original outer0/complete/inputs equality/no timeout/known groups absent. First unexpected failure retain-stop, no automatic retry. No product publication/appbuild/Workerchange/staging resource mutation from this local validation. Next reviews actual retained results before packaging or further performance comparison.
